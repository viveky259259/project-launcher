{
  "name": "plauncher-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "E2E integration tests for plauncher-backend API",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
